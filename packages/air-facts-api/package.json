{
  "name": "air-facts-api",
  "version": "0.1.0",
  "private": true,
  "description": "AIR Facts API - Cloudflare Worker for crowdsourced fact database",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.0",
    "wrangler": "^4.0.0"
  }
}
